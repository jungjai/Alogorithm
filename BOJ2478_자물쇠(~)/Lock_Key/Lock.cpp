#include<stdio.h>

#pragma warning (disable : 4996)

int main()
{
	freopen("input.txt", "r", stdin);

	int su;
	int can[500];
	
	int shift[2];

	scanf("%d", &su);

	for (int i = 0; i < su; i++)
		scanf("%d", can[i]);	
		

	int lock, max, len;

	return 0;
}